import { ColorElementDirective } from './color-element.directive';

describe('ColorElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorElementDirective();
    expect(directive).toBeTruthy();
  });
});
